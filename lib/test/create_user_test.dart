import 'package:dart_blocks/softcorp_client.dart';
import 'package:test/test.dart';

void main() {
  test('Should return a valid newly created user', () async {
    /*SoftcorpClient softcorpClient = SoftcorpClient(apiKey: apiKey, apiUrl: "https://stage.api.softcorp.io:443");
    try {
      await SoftcorpClient.initialize();
    }catch(e){
      print(e);
    }
    var userResp = SoftcorpClient.userClient?.create(email: "my email :-)");
    print(userResp.toString());
     */
  });
}